#include <print>
#include <vector>

class Matrix{
public:
	Matrix();
};

bool compare(Matrix &, Matrix &);

std::vector<int>::iterator change_val(int, std::vector<int>::iterator);

int main(){
	std::print("DONE!");
}