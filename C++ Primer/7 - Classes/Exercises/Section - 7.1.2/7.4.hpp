#include <iostream>
#include <string>

struct Person{
    // Member functions
    std::string getName() const {return name;}
    std::string getAddress() const {return name;}
    // data members
    std::string name;
    std::string address;
};

